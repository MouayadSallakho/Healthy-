# Barbell Kitchen — environment variables (template). Copy to `.env.local`.

# Backend API base URL (Healthy Menu API): origin + project path WITHOUT the
# `/api` prefix. The client appends `/api` automatically.
#   Production: https://technosolutions.sy/barbell-api
#   Local dev:  http://127.0.0.1:8000
NEXT_PUBLIC_API_BASE_URL=https://technosolutions.sy/barbell-api
